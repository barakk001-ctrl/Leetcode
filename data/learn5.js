window.LC_LEARN = window.LC_LEARN || {};
window.LC_LEARN[5] = {
  sections: [
    {
      title: "The Recursion Mental Model: Solve Left, Solve Right, Combine",
      body: `Almost every tree problem reduces to the same shape: solve the problem for the left subtree, solve it for the right subtree, then combine those two answers with the current node. The trick that makes this feel effortless is the "recursive leap of faith": when you write maxDepth(node.left), you assume it returns the correct answer for that whole subtree. You never trace into it mentally. You only design two things — the base case (almost always: null returns 0, false, or null) and the combine step at the current node.\n\nMaximum Depth is the canonical 3-liner: if the node is null the depth is 0; otherwise it is 1 plus the larger of the two subtree depths. That is the entire solution. Compare that to how you would solve it iteratively and you see why recursion is the default tool for trees — the call stack does the bookkeeping for you.\n\nRecognition: if the answer for a node can be computed from the answers for its children, write the recursive skeleton first and only fill in the combine logic. Depth, sum, count, "is balanced", "is same tree", "invert" — all the same skeleton.`,
      kotlin: `fun maxDepth(root: TreeNode?): Int {
    if (root == null) return 0                 // base case
    val left = maxDepth(root.left)             // trust it
    val right = maxDepth(root.right)           // trust it
    return 1 + maxOf(left, right)              // combine
}`
    },
    {
      title: "The Three DFS Orders and What Each Is For",
      body: `Preorder, inorder, and postorder all visit every node exactly once — the only difference is when you process the current node relative to its children. That timing is not cosmetic; it decides which problems each order can solve.\n\nPreorder (node, left, right) processes the parent first, so it is the order for copying or serializing a tree: you must write the root before you can reconstruct its children ("Serialize and Deserialize Binary Tree", "Construct Tree from Preorder..."). Inorder (left, node, right) on a Binary Search Tree emits values in sorted order — this single fact powers a whole family of BST problems. Postorder (left, right, node) processes children before the parent, so it is the order whenever a node's answer depends on completed child answers: computing heights, subtree sums, "Balanced Binary Tree", "Diameter", and safe deletion (free children before the parent).\n\nRecognition: ask "do I need child results before I can handle this node?" If yes, postorder. "Do I need to record the node before descending?" Preorder. "Is it a BST and something about sorted order or ranking?" Inorder.`
    },
    {
      title: "BFS / Level Order: Queue Plus a Level-Size Snapshot",
      body: `BFS replaces the call stack with an explicit FIFO queue. You seed it with the root, and each round you drain exactly the nodes that were in the queue at the start of the round — that snapshot is one level. The key line is val size = queue.size taken before the inner loop: children you enqueue during the round belong to the next level and must not be processed this round.\n\nRecognition is unusually mechanical: if the problem statement says "level", "layer", "depth d", "zigzag", "right side view", or "nearest/shortest" (minimum depth, closest leaf), it is BFS. DFS can often be forced to work with a depth parameter, but BFS gives you the level boundary for free and finds the shallowest answer first, which is exactly what "minimum" questions want.\n\nComplexity is the same O(n) time as DFS, but space is O(w) where w is the maximum width of the tree — up to n/2 for the bottom level of a complete tree. So BFS trades stack depth for queue width.`,
      kotlin: "fun levelOrder(root: TreeNode?): List<List<Int>> {\n    val result = mutableListOf<List<Int>>()\n    if (root == null) return result\n    val queue = ArrayDeque<TreeNode>().apply { add(root) }\n    while (queue.isNotEmpty()) {\n        val size = queue.size                  // snapshot this level\n        val level = mutableListOf<Int>()\n        repeat(size) {\n            val node = queue.removeFirst()\n            level.add(node.`val`)\n            node.left?.let { queue.add(it) }\n            node.right?.let { queue.add(it) }\n        }\n        result.add(level)\n    }\n    return result\n}"
    },
    {
      title: "The BST Invariant: Validate with Ranges, Exploit with Inorder",
      body: `The BST property is stronger than most people first read it: it is not "left child < node < right child" — it is EVERYTHING in the left subtree < node < EVERYTHING in the right subtree. The classic trap in Validate Binary Search Tree is checking only parent-child pairs; a tree like 5 -> (right) 7 -> (left) 4 passes local checks but 4 sits in 5's right subtree while being smaller than 5. The fix is to pass an allowed (min, max) range down the recursion: the root gets (-inf, +inf), a left child inherits (min, parent), a right child inherits (parent, max). Each node checks itself against its range in O(1).\n\nThe second half of the invariant is the payoff: an inorder traversal of a BST visits values in strictly ascending order. That turns "Kth Smallest Element in a BST" into "run inorder, stop after k visits", makes duplicate/ordering checks a one-pass comparison against the previous visited value, and is an alternative way to validate the BST itself.\n\nRecognition: the moment a problem says BST, your two levers are range-passing on the way down and sorted inorder on the way through. Also remember BST search/insert is O(h), not O(log n) — that only holds if the tree is balanced.`,
      kotlin: "fun isValidBST(root: TreeNode?): Boolean =\n    validate(root, Long.MIN_VALUE, Long.MAX_VALUE)\n\nprivate fun validate(node: TreeNode?, min: Long, max: Long): Boolean {\n    if (node == null) return true\n    val v = node.`val`.toLong()\n    if (v <= min || v >= max) return false\n    return validate(node.left, min, v) && validate(node.right, v, max)\n}"
    },
    {
      title: "Complexity on Trees: O(n) Time, O(h) Space, and the Stack-Overflow Reality",
      body: `Time on trees is almost always O(n): every traversal visits each node exactly once and does O(1) work per node (watch out for accidental O(n) work per node, like calling a height() helper inside a recursive function — that is how Balanced Binary Tree becomes O(n^2) instead of O(n)).\n\nSpace is where interviews probe. Recursive DFS is not O(1): the call stack holds one frame per level of the current path, so space is O(h) where h is the tree height. Balanced tree: h = log n, so O(log n). Degenerate tree (every node has one child — effectively a linked list): h = n, so O(n). Always state both cases; saying "O(log n) space" unconditionally is a red flag.\n\nAs a backend dev you already know why this matters in production: JVM default thread stack is around 512KB-1MB, so a tree (or object graph) a few hundred thousand nodes deep will throw StackOverflowError. The fixes are an iterative traversal with an explicit stack (mechanical translation: push root, pop, process, push children), Morris traversal for O(1) space, or Kotlin's tailrec where the recursion happens to be tail-shaped. In an interview, writing the recursive version and then saying "for a degenerate tree in production I would switch to an explicit stack" is exactly the signal they want.`,
      kotlin: "fun preorderIterative(root: TreeNode?): List<Int> {\n    val result = mutableListOf<Int>()\n    val stack = ArrayDeque<TreeNode>()\n    root?.let { stack.addLast(it) }\n    while (stack.isNotEmpty()) {\n        val node = stack.removeLast()\n        result.add(node.`val`)\n        node.right?.let { stack.addLast(it) }  // right first,\n        node.left?.let { stack.addLast(it) }   // so left pops first\n    }\n    return result\n}"
    }
  ]
};
